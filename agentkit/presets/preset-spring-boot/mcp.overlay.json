{
  "servers": [
    {
      "name": "postgres",
      "enabledByDefault": false,
      "required": false,
      "transport": "stdio",
      "command": "sh",
      "args": [
        "-lc",
        "if [ -f .env.mcp ]; then set -a; . ./.env.mcp; set +a; fi; exec ${REPO_ROOT}/.mcp-cache/runtime/node_modules/.bin/mcp-server-postgres \"$POSTGRES_URL\""
      ],
      "cwd": "${REPO_ROOT}",
      "startupTimeoutSec": 90,
      "approval": "prompt",
      "envVars": ["POSTGRES_URL"]
    }
  ]
}
