## Project Guidance (Angular)

An Angular (TypeScript) frontend. Conventions live in
`.agents/skills/stack-angular/`. Use the Angular CLI (`ng`) and `npm` scripts. Keep
components presentational and logic in services; lazy-load feature routes; type
everything.
