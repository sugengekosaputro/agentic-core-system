# Quality

- **Virtual Assistant:** QA (`virtual-assistant-qa`) — *reserved; scaffolded per project when needed.*
- **Purpose:** Define how the product is verified against its requirements.
- **Document types:** test strategy, test plans, test cases, coverage/QA reports.
- **Naming:** `test-strategy.md`, `NNN-<feature>-testplan.md`.
- **Inputs / outputs:** reads `requirements/` and `architecture/`; reports results.
