# Architecture Decision Records — Index

Only `accepted` ADRs are in force. Superseded/deprecated/rejected entries are kept
for history. Read this index first, then load only the ADR bodies you need.

| ID | Title | Status |
|----|-------|--------|
| _none yet_ | | |

Conventions:
- File name: `NNNN-short-title.md` (zero-padded, monotonic, never reused).
- Replacing a decision: add a new ADR, set the old one's status to `superseded`
  with `Superseded-by: ADR-NNNN`, and update this table.
- Template: `template.md`.
