# Local MCP credentials. Copy to .env.mcp (gitignored). Names only here; never commit values.
# The base servers (context7, filesystem) need no secrets.
# Presets add their own variables here when they introduce servers, e.g.:
#   GITHUB_PERSONAL_ACCESS_TOKEN=
#   POSTGRES_URL=
