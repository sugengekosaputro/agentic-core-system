# Project memory (journal)

Working continuity for this project — where we are, what was tried, and useful
pointers. **Read on demand** (e.g. when resuming multi-session work); this file is
**not** loaded every turn, so it does not cost tokens during normal work.

Keep it terse and pointer-based. Record **decisions** as ADRs
(`docs/architecture/adr/`) and **facts** in `.agents/project.json` — not here.
Summarize or rotate when it grows large.

## Current state
_(The only part you normally need on resume. Keep it short.)_

-

## Log
_(Append-only, newest first: `YYYY-MM-DD — what changed / what was tried — pointer`)_

-
